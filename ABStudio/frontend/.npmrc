# Pins the public npm registry for this package.
#
# npm reads project config from the package root, so the repository-root
# .npmrc does NOT apply when running npm from this directory. This file is
# required here (not just at the repo root) so that `npm install` / `npm ci`
# never fall back to a corporate registry inherited from ~/.npmrc.
registry=https://registry.npmjs.org/
