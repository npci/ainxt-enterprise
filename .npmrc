# Repo-level npm configuration
# Explicitly pins the public npm registry so external contributors can
# run `npm install` without access to any internal package registry.
# This overrides any system-level (~/.npmrc) registry setting for this repo.
registry=https://registry.npmjs.org/
