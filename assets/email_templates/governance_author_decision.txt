{%- set _title = "AiNxt - Governance " ~ ("Approved" if decision == "approved" else "Changes Requested") ~ ": " ~ (run.jira_key or run.id) -%}
{{ _title }}
{{ "=" * _title|length }}

Hi {{ author.name or author.email }},

{% if decision == "approved" -%}
Good news - all governance domains for your SDLC run have been APPROVED.
The run can now proceed to the next stage.
{%- else -%}
The {{ domain }} governance team has REQUESTED CHANGES on your SDLC run and
sent it back to you. Please address the feedback below and re-submit for review.
{%- endif %}

{% if decision != "approved" and comment %}
Reviewer feedback{% if decided_by %} ({{ decided_by }}){% endif %}
-----------------
{{ comment }}
{% endif %}
Run details
-----------
  Run ID:        {{ run.id }}
{%- if run.jira_key %}
  Jira:          {{ run.jira_key }}
{%- endif %}
  Repository:    {{ run.repo or "-" }}
  Branch:        {{ run.branch or "-" }}
{%- if decision != "approved" %}
  Domain:        {{ domain }}
{%- endif %}
  Decision (IST):{{ decided_at_ist }}
{% if run_url %}
{% if decision == "approved" %}View the run:{% else %}Address the feedback:{% endif %}
  {{ run_url }}
{% endif %}

--
Generated by AiNxt · {{ platform_name }} SDLC Governance
This is an automated message - do not reply to this email.
