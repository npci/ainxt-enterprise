apiVersion: 1

datasources:
  - name: Loki
    uid: loki
    type: loki
    access: proxy
    # Uses host-mapped Loki on :3100 (works on Docker Desktop for macOS).
    url: http://loki:3100
    isDefault: false
    editable: false

